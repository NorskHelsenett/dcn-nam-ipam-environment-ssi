apiVersion: v1
kind: Secret
metadata:
  name: nam-ipam-environment-ssi-{{ .Values.settings.infrastructure }}-secrets
  namespace: ssi
type: Opaque
stringData:
  secrets.yaml: |
    ---
    NAM_TOKEN: {{ .Values.credentials.namToken | quote }}
    SPLUNK_TOKEN: {{ .Values.credentials.splunkToken | quote }}
    IPAM_TOKEN: {{ .Values.credentials.ipamToken | quote }}